import styled from 'styled-components';

const Container = styled.div`
    margin-bottom: 16px;
`;

const Composer = styled.div`
    display: flex;
    flex-wrap: wrap;
`;

export default { Container, Composer };
